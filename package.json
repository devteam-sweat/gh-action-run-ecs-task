{
  "name": "gh-action-run-ecs-task",
  "version": "0.2.0",
  "main": "lib/index.js",
  "scripts": {
    "build": "npx tsc",
    "pack": "ncc build lib/index.js -o dist",
    "precommit": "lint-staged",
    "test": "vitest run --coverage",
    "all": "npm run build && npm run pack && npm run test"
  },
  "keywords": [],
  "author": "Gus Vine",
  "license": "ISC",
  "description": "github action to run an ecs task",
  "devDependencies": {
    "@vercel/ncc": "^0.44.1",
    "@vitest/coverage-v8": "^4.0.0",
    "aws-sdk-client-mock": "^4.1.0",
    "aws-sdk-client-mock-vitest": "^7.1.0",
    "ts-loader": "^9.6.2",
    "typescript": "^6.0.3",
    "vitest": "^4.0.0"
  },
  "dependencies": {
    "@actions/core": "^3.0.1",
    "@aws-sdk/client-ecs": "^3.1079.0",
    "@aws-sdk/client-ssm": "^3.1085.0"
  }
}
